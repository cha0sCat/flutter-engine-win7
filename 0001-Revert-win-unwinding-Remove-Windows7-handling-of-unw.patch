diff --git a/runtime/platform/unwinding_records_win.cc b/runtime/platform/unwinding_records_win.cc
index da871a640cb..1e02456d437 100644
--- a/runtime/platform/unwinding_records_win.cc
+++ b/runtime/platform/unwinding_records_win.cc
@@ -7,6 +7,10 @@
 #include "platform/assert.h"
 #include "platform/globals.h"
 
+#if defined(UNWINDING_RECORDS_WINDOWS_HOST)
+#include <windows.h>
+#endif
+
 namespace dart {
 
 #if defined(NEED_WINDOWS_UNWINDING_RECORDS)
@@ -29,6 +33,43 @@ intptr_t UnwindingRecordsPlatform::SizeInBytes() {
 // as it is the only client of these methods (e.g., _not_ in gen_snapshot).
 #if defined(UNWINDING_RECORDS_WINDOWS_HOST)
 
+namespace {
+
+using AddGrowableFunctionTableFn = DWORD(WINAPI*)(PVOID*,
+                                                 PRUNTIME_FUNCTION,
+                                                 DWORD,
+                                                 DWORD,
+                                                 ULONG_PTR,
+                                                 ULONG_PTR);
+using DeleteGrowableFunctionTableFn = VOID(WINAPI*)(PVOID);
+
+struct GrowableFunctionTableFuncs {
+  AddGrowableFunctionTableFn add = nullptr;
+  DeleteGrowableFunctionTableFn del = nullptr;
+};
+
+const GrowableFunctionTableFuncs& GetGrowableFunctionTableFuncs() {
+  static GrowableFunctionTableFuncs funcs = []() {
+    GrowableFunctionTableFuncs result;
+    HMODULE ntdll = GetModuleHandleW(L"ntdll.dll");
+    if (ntdll == nullptr) {
+      return result;
+    }
+    result.add = reinterpret_cast<AddGrowableFunctionTableFn>(
+        GetProcAddress(ntdll, "RtlAddGrowableFunctionTable"));
+    result.del = reinterpret_cast<DeleteGrowableFunctionTableFn>(
+        GetProcAddress(ntdll, "RtlDeleteGrowableFunctionTable"));
+    if ((result.add == nullptr) != (result.del == nullptr)) {
+      result.add = nullptr;
+      result.del = nullptr;
+    }
+    return result;
+  }();
+  return funcs;
+}
+
+}  // namespace
+
 void UnwindingRecordsPlatform::RegisterExecutableMemory(
     void* start,
     intptr_t size,
@@ -43,12 +84,16 @@ void UnwindingRecordsPlatform::RegisterExecutableMemory(
     intptr_t size,
     void* records_start,
     void** pp_dynamic_table) {
+  const auto& funcs = GetGrowableFunctionTableFuncs();
+  if (funcs.add == nullptr) {
+    return;
+  }
   CodeRangeUnwindingRecord* record =
       reinterpret_cast<CodeRangeUnwindingRecord*>(records_start);
   RELEASE_ASSERT(record->magic == kUnwindingRecordMagic);
   uword start_num = reinterpret_cast<intptr_t>(start);
   uword end_num = start_num + size;
-  DWORD status = RtlAddGrowableFunctionTable(
+  DWORD status = funcs.add(
       pp_dynamic_table,
       /*FunctionTable=*/
       reinterpret_cast<PRUNTIME_FUNCTION>(record->runtime_function),
@@ -62,7 +107,11 @@ void UnwindingRecordsPlatform::RegisterExecutableMemory(
 }
 
 void UnwindingRecordsPlatform::UnregisterDynamicTable(void* p_dynamic_table) {
-  RtlDeleteGrowableFunctionTable(p_dynamic_table);
+  const auto& funcs = GetGrowableFunctionTableFuncs();
+  if (funcs.del == nullptr) {
+    return;
+  }
+  funcs.del(p_dynamic_table);
 }
 
 #endif  // defined(UNWINDING_RECORDS_WINDOWS_HOST)
diff --git a/runtime/vm/unwinding_records_win.cc b/runtime/vm/unwinding_records_win.cc
index 2ab9c4d27e1..f67ef9916e4 100644
--- a/runtime/vm/unwinding_records_win.cc
+++ b/runtime/vm/unwinding_records_win.cc
@@ -6,6 +6,10 @@
 
 #include "platform/globals.h"
 
+#if defined(UNWINDING_RECORDS_WINDOWS_HOST)
+#include <windows.h>
+#endif
+
 namespace dart {
 
 #if defined(NEED_WINDOWS_UNWINDING_RECORDS)
@@ -101,9 +105,51 @@ const void* UnwindingRecords::GenerateRecordsInto(intptr_t offset,
 #endif  // defined(UNWINDING_RECORDS_WINDOWS_PRECOMPILER)
 
 #if defined(UNWINDING_RECORDS_WINDOWS_HOST)
+
+namespace {
+
+using AddGrowableFunctionTableFn = DWORD(WINAPI*)(PVOID*,
+                                                 PRUNTIME_FUNCTION,
+                                                 DWORD,
+                                                 DWORD,
+                                                 ULONG_PTR,
+                                                 ULONG_PTR);
+using DeleteGrowableFunctionTableFn = VOID(WINAPI*)(PVOID);
+
+struct GrowableFunctionTableFuncs {
+  AddGrowableFunctionTableFn add = nullptr;
+  DeleteGrowableFunctionTableFn del = nullptr;
+};
+
+const GrowableFunctionTableFuncs& GetGrowableFunctionTableFuncs() {
+  static GrowableFunctionTableFuncs funcs = []() {
+    GrowableFunctionTableFuncs result;
+    HMODULE ntdll = GetModuleHandleW(L"ntdll.dll");
+    if (ntdll == nullptr) {
+      return result;
+    }
+    result.add = reinterpret_cast<AddGrowableFunctionTableFn>(
+        GetProcAddress(ntdll, "RtlAddGrowableFunctionTable"));
+    result.del = reinterpret_cast<DeleteGrowableFunctionTableFn>(
+        GetProcAddress(ntdll, "RtlDeleteGrowableFunctionTable"));
+    if ((result.add == nullptr) != (result.del == nullptr)) {
+      result.add = nullptr;
+      result.del = nullptr;
+    }
+    return result;
+  }();
+  return funcs;
+}
+
+}  // namespace
+
 // Special exception-unwinding records are put at the end of executable
 // page on Windows for 64-bit applications.
 void UnwindingRecords::RegisterExecutablePage(Page* page) {
+  const auto& funcs = GetGrowableFunctionTableFuncs();
+  if (funcs.add == nullptr) {
+    return;
+  }
   ASSERT(page->is_executable());
   ASSERT(sizeof(CodeRangeUnwindingRecord) <=
          UnwindingRecordsPlatform::SizeInBytes());
@@ -115,7 +161,7 @@ void UnwindingRecords::RegisterExecutablePage(Page* page) {
            unwinding_record_offset) CodeRangeUnwindingRecord();
   InitUnwindingRecord(unwinding_record_offset, record, page->memory_->size());
   RELEASE_ASSERT(record->magic == kUnwindingRecordMagic);
-  DWORD status = RtlAddGrowableFunctionTable(
+  DWORD status = funcs.add(
       /*DynamicTable=*/&record->dynamic_table,
       /*FunctionTable=*/
       reinterpret_cast<PRUNTIME_FUNCTION>(record->runtime_function),
@@ -137,7 +183,11 @@ void UnwindingRecords::UnregisterExecutablePage(Page* page) {
           reinterpret_cast<uint8_t*>(page->memory_->start()) +
           unwinding_record_offset);
   RELEASE_ASSERT(record->magic == kUnwindingRecordMagic);
-  RtlDeleteGrowableFunctionTable(record->dynamic_table);
+  const auto& funcs = GetGrowableFunctionTableFuncs();
+  if (funcs.del == nullptr) {
+    return;
+  }
+  funcs.del(record->dynamic_table);
 }
 #endif  // defined(UNWINDING_RECORDS_WINDOWS_HOST)
 
